import { Typography } from 'antd';

const { Title, Text } = Typography;

export default function IncidentDetailPage() {
  return (
    <div>
      <Title level={3}>Incident Detail</Title>
      <Text type="secondary">This page will be built in the next prompt.</Text>
    </div>
  );
}
